<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="1280" d:DesignHeight="800"
        Width="1280" Height="800"
        x:Class="PerfViewSharp.UI.MainWindow"
        Title="PerfViewSharp - 下一代智能性能分析器"
        Background="#1E1E1E">
    
    <Window.Styles>
        <Style Selector="Button.ActionBtn">
            <Setter Property="Background" Value="#4A90E2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="CornerRadius" Value="4"/>
        </Style>
        <Style Selector="Button.ActionBtn:pointerover /template/ ContentPresenter#PART_ContentPresenter">
            <Setter Property="Background" Value="#357ABD"/>
        </Style>
        <Style Selector="TextBox.SearchBox">
            <Setter Property="Background" Value="#2D2D2D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#444444"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="15,5"/>
            <Setter Property="CornerRadius" Value="20"/>
        </Style>
    </Window.Styles>

    <DockPanel>
        <!-- 顶部标题与搜索栏 -->
        <Border DockPanel.Dock="Top" Height="65" Background="#181818" Padding="20,0" BorderBrush="#333333" BorderThickness="0,0,0,1">
            <Grid ColumnDefinitions="Auto, *, Auto">
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="🚀 PerfView" Foreground="White" FontSize="20" FontWeight="Bold"/>
                    <TextBlock Text="Sharp" Foreground="#4A90E2" FontSize="20" FontWeight="Bold"/>
                </StackPanel>

                <!-- 全局搜索框 (中文提示) -->
                <TextBox Grid.Column="1" Name="GlobalSearchBox" Classes="SearchBox" 
                         Watermark="输入方法名进行全景搜索 (支持即时高亮)..." 
                         Width="500" HorizontalAlignment="Center" Margin="20,0"/>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Spacing="10">
                    <TextBlock Text="总事件: 1.2M" Foreground="#888888" FontSize="12"/>
                    <Border Width="1" Height="20" Background="#333333"/>
                    <TextBlock Text="当前 CPU: 12%" Foreground="#4CAF50" FontSize="12" FontWeight="Bold"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 底部状态栏 (中文操作引导) -->
        <Border DockPanel.Dock="Bottom" Height="30" Background="#007ACC" Padding="15,0">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Spacing="20">
                <TextBlock Text="💡 操作提示:" FontSize="11" FontWeight="Bold" Foreground="White"/>
                <TextBlock Text="双指捏合/滚动: 缩放时间轴" FontSize="11" Foreground="White"/>
                <TextBlock Text="左键按住拖拽: 平移视野" FontSize="11" Foreground="White"/>
                <TextBlock Text="双击方块: 跳转至源代码" FontSize="11" Foreground="White"/>
                <Separator Width="10" Background="Transparent"/>
                <TextBlock Text="✅ 渲染引擎已就绪" FontSize="11" Foreground="#AAFFFFFF"/>
            </StackPanel>
        </Border>

        <!-- 主内容区 -->
        <Grid ColumnDefinitions="*, 340">
            <Border Grid.Column="0" Margin="12" Background="#050505" CornerRadius="8" BorderBrush="#333333" BorderThickness="1" ClipToBounds="True">
                <Grid RowDefinitions="*, 100">
                    <!-- 火焰图主视口 -->
                    <Grid Name="MainGrid" Grid.Row="0"/>
                    
                    <!-- 鹰眼图全景导航 (中文标注) -->
                    <Border Grid.Row="1" Background="#111111" BorderBrush="#333333" BorderThickness="0,1,0,0">
                        <Grid>
                            <TextBlock Text="全景导航" FontSize="10" Foreground="#444444" Margin="10,5" VerticalAlignment="Top"/>
                            <Grid Name="MinimapGrid"/>
                        </Grid>
                    </Border>
                </Grid>
            </Border>

            <!-- 右侧统计面板 -->
            <Border Grid.Column="1" Margin="0,12,12,12" Background="#252526" CornerRadius="8" BorderBrush="#333333" BorderThickness="1" Padding="20">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Top" Spacing="15">
                        <TextBlock Text="📊 方法详细统计" FontSize="11" Foreground="#4A90E2" FontWeight="Bold" LetterSpacing="1"/>
                        <Border Background="#1E1E1E" CornerRadius="6" Padding="15">
                            <StackPanel Spacing="10">
                                <TextBlock Name="MethodNameLabel" Text="请选择一个方法块..." FontSize="15" Foreground="White" FontWeight="SemiBold" TextWrapping="Wrap"/>
                                <ProgressBar Name="CpuProgressBar" Height="4" Minimum="0" Maximum="100" Value="0" Background="#111111" Foreground="#4CAF50"/>
                                <Grid ColumnDefinitions="*,*">
                                    <TextBlock Name="TotalTimeLabel" Grid.Column="0" Text="0.0 ms" FontSize="12" Foreground="#AAAAAA"/>
                                    <TextBlock Name="PercentLabel" Grid.Column="1" Text="0.0 %" FontSize="12" Foreground="#4CAF50" HorizontalAlignment="Right"/>
                                </Grid>
                            </StackPanel>
                        </Border>
                        
                        <Separator Height="1" Background="#333333"/>
                        <TextBlock Text="🔥 当前视图热点路径" FontSize="11" Foreground="#888888" FontWeight="Bold"/>
                        
                        <ListBox Name="HotspotList" Background="Transparent" BorderThickness="0">
                            <!-- 动态加载 -->
                        </ListBox>
                    </StackPanel>

                    <Button DockPanel.Dock="Bottom" Name="JumpButton" Classes="ActionBtn" Content="定位源代码" HorizontalAlignment="Stretch" Margin="0,10,0,0"/>
                    <Border/>
                </DockPanel>
            </Border>
        </Grid>
    </DockPanel>
</Window>
